import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        padding: 10,
    },
    text: {
        fontFamily: 'Sailec-Medium, Arial, Helvetica, sans-serif',
    },
});
