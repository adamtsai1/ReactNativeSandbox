import React from 'react';
import { Text, View } from 'react-native';

export const TimeOffListItem = ({ item }) => {
    return (
        <View>
            <Text>test</Text>
        </View>
    );
};
